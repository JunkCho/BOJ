def P_sum(a):
    t=0
    for i in range(a):
        t += i+1
    return t
import itertools

A = int(input())
for j in range(A):
    temp=0
    B = input()
    import itertools
    G = itertools.groupby(B)
    g= [k for k, v in G]
    g
    len(g)
    b = B.split('X')
    b
    len(b)
    for k in range(len(b)):  #덩어리의 개수 규칙만 찾자!!!!!
        temp +=P_sum(b[k].count('O'))
    print(temp)
